# templates/pod-manager-service.yaml
{{- if .Values.podManager.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.module }}-service
  labels:
    {{- include "label-generator" . | nindent 4 }}
spec:
  type: {{ .Values.service.type | default "ClusterIP" }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort | default "http" }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.module }} #  Doit correspondre au label du pod-manager deployment
{{- end }}